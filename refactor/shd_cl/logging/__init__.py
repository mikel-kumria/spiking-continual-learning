"""Logging helpers: W&B wrappers and reusable plots (hidden raster, histograms).

This subpackage is named ``logging`` but never imports the stdlib ``logging``
module; console output uses ``print`` to avoid any shadowing confusion.
"""
